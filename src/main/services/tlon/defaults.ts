/**
 * Tlon ingest defaults — seed contents for a new knowledge base's index.md and
 * log.md. index.md is rebuilt programmatically as a document map once sources
 * are extracted; these are only the empty-state placeholders.
 */

export const DEFAULT_INDEX_MD = `# Index

_No documents indexed yet. Add source files to populate this knowledge base._
`

export const DEFAULT_LOG_MD = `# Ingest Log
`
