node_modules
dist
build
out

# TypeScript build cache
*.tsbuildinfo
.halo

# Built-in digital humans bundle (build artifact, generated by
# scripts/sync-builtin-apps.mjs from the SSOT declared in product.json).
# Each product variant points at its own SSOT so this directory is per-build,
# never checked in.
resources/builtin-apps/

# Portable Git archive for Windows builds (downloaded by scripts/prepare-binaries.mjs)
resources/git-bash/

# Release notes temp files
.release-notes-current.md
.release-notes-summary.md

# Local environment variables (secrets)
.env.local

# E2E test results
tests/e2e/results/
tests/e2e/fixtures/.e2e-seed-tmp/

# Perf measurement output: per-run residue, except the two frozen baselines the
# gate's self-test grades itself against and every published number reconciles
# to. Those are tracked so neither a fresh clone nor `git clean` can leave the
# self-test with no fixtures — a check whose inputs can vanish proves nothing.
# `/*` rather than `/`: git cannot re-include anything under an excluded
# directory, so excluding the directory itself makes the negations below silently
# match nothing.
tests/perf/results/*
!tests/perf/results/report-snapshot/
!tests/perf/results/final-frozen/
tests/perf/results/final-frozen/s10-screenshots/
# Reproduced from tests/perf/fixtures/generate.py; manifest.json is tracked instead.
tests/perf/fixtures/generated/
test-results/

app.log
# Local docs
local_docs/

# Local plugins
halo-local/
product.json
product.local.json

# Active product variant pointer (per-machine, managed by `npm run product`)
.product-source

# Local debug scripts
vscode-debug/

# Local deploy scripts
scripts/deploy_local_*.sh

# Backup files
*.backup
*copy.*

# Website (landing page, managed separately)
website/

# Capacitor mobile build output
dist-mobile/

# Android build artifacts (Gradle caches, APKs)
android/app/src/main/assets/public/*
android/app/build/
android/.gradle/
android/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
*.apk
*.keystore

# OS files
.DS_Store
RELEASE_NOTES.md
# temp

# Internal digital human skills (wekm)
.claude/skills/wekm-*

# Halo Agent SDK (managed in separate private repo)
src/sdk/
/temp/

# Local multi-node federation test cluster (isolated node data + logs + secrets)
.cluster/

# Stray KB wiki artifacts (real wikis live under ~/.halo, never in-repo)
/wiki/
# Personal marketing/intro drafts (not product code)
/intro-doc/

# Local git worktrees created for isolated work
.claude/worktrees/
.cluster*